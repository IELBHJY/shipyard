shipyard2有bug，运行时偶尔会出现平板车上任务时间不连续
描述：某一个超重任务前一个任务的开始时间大于超重任务，或者任务开始时间加上任务完成时间和空载行驶时间大于任务开始时间
原因：超重任务时间更新机制有问题，即find函数有问题，需要修改。2018/10/3 10:48.pm in WuHan
bug已经修改，find函数重新写，并未出现bug了。

shipyard1有bug，即分支定界中，利用信息构建模型时，会出现模型的解是不可行的，除了整数约束之外的还是不可行，测试发现，是因为模型中的
变量号和paths中的路径不是一一对应的，导致模型求的变量=1的对应路径是错的。

b&p过程完成，在task10 truck2  和task12 truck3 上均可以求出最优解，接下来的工作是将加列部分细化。
可以考虑结合禁忌搜索算法，可以为b&p 提供初始解、提供更紧的上界、还可以将禁忌搜索求的结果中找出检验数小于0的方案加入模型，可以快速找到有效的列。

